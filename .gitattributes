# Snapshot files are compared byte-for-byte against interpreter output, which
# always uses LF. Without this, cloning on Windows (core.autocrlf=true) rewrites
# them to CRLF and every snapshot test fails on an otherwise untouched checkout.
nfsharp/test/snapshots/*.txt text eol=lf
